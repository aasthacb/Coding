-> find recursive solution -> visualise the tree -> memoise it
do not add base cases to memo[] (or dp[]), return

func :
#check if key in memo
#base cases
#recursive return (return fib(n-1,dp) + fib(n-2,dp))  #remember to pass memo
